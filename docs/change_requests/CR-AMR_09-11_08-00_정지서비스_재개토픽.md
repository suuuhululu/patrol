# [AMR] 순찰 정지 서비스와 재개 토픽 연계 검토

- 최초 작성: 2026-09-11 08:00 KST (UTC+09:00)
- 작성 팀: AMR
- 검토 대상: 비전, 관제, System monitor
- 근거: 2026-09-11 현재 사용자 지시 및 후속 정정. 첫 유효 bbox에서 false 서비스 즉시 요청, 현재 위치 정지 후 완료 응답, 시스템 모니터의 감지 보고 STORED 응답 뒤 true 토픽 발행. 지정 Python 코드만 참조.
- 상태: AMR 구현 승인 및 반영, 로컬 검사 완료. 상대 팀 합의·코드 반영·실기 통합 검증은 미완료.

## 이번 구현의 연계 값

| 대상 | 형식 | 처리 |
|---|---|---|
| `/<robot>/patrol_stop` | `std_srvs/srv/SetBool` | 요청 data=false. Nav2 종료와 새 odom 정지 확인 후 success=true. 실패는 success=false와 message |
| `/<robot>/patrol_resume` | `std_msgs/msg/Bool` | true로 기존 단계 재개. false는 재개 보류이며 정지 서비스 대체 아님 |

기본 robot 값은 기존 코드의 robot1·robot6이다. 이름은 `--stop-service`, `--resume-topic`으로 지정할 수 있다. 서비스 data=true와 순찰 준비·도킹 중 요청은 실패 응답한다. 최근 2개 수신 이벤트는 메모리에 유지한다.

사용자가 추가 지정한 AMR 로컬 카메라 코드 `vision_node_v2.py`를 연결했다. 첫 유효 bbox에서 정지를 요청하고 정지 성공 및 기존 보고 hold 충족 후 기존 ReportDetection 서비스를 호출한다. STORED 응답일 때만 재개한다. 앞선 수동 재개 선택은 이 최신 사용자 결정으로 대체했다. PC 4 CCTV 코드는 변경하지 않았다.

공용 메시지 패키지는 기존 ReportDetection과 표준 SetBool·Bool을 사용하므로 변경 불필요하다. System monitor의 보고 서비스 응답 형식도 변경하지 않아 모니터 코드 변경은 불필요하다. 관제 코드는 이번 연결에 직접 사용하지 않아 변경하지 않았으며, 기존 운영 경로와의 연계 여부 검토가 필요하다. 상대 팀 검토 및 실기 통합 검증은 미완료이며 공용 계약 문서 수정·통합 합의 완료를 뜻하지 않는다.

구현 파일과 코드별 흐름, 검사 범위: [event_check 구현 대조](../event_check.md).

2026-09-11 후속 결정: 사용자 지시로 AMR 로컬 vision_node_v2.py의 기준 로봇을 robot6에서 robot1로 변경했다. 카메라·odom 구독, 정지 서비스 대상, 재개 토픽 및 ReportDetection.robot_id가 robot1을 사용한다. 인터페이스 타입은 동일하다. 관련 비전 연계 검사 10개 통과, 실제 robot1 카메라·주행 통합시험은 미실행이다. 영향 단위는 AMR 로컬 감지와 보고 수신 System monitor이며 모니터의 서비스 형식 변경은 불필요하다.
