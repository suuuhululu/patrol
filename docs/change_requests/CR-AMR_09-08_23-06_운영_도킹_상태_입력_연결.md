# [AMR] 운영·도킹 상태 입력 연결 검토

- 상태: safety 임시 연결 구현 / 합병 시 명시적 입력 계약 검토 대기
- 최초 작성 시각: 2026-09-08 23:06 KST
- 요청자·요청 단위: AMR
- 대상 단위 및 로봇: AMR mission 담당·patrol_amr_safety 담당, robot1·robot6; 관제·System monitor 수신 영향 검토
- 관련 TBD ID: TBD-AMR-005, TBD-IF-003
- 기준: [interfaces.md 4절](../interfaces.md), [flowchart 9.5절](../amr_patrol_safety_flowchart.md), [기존 종단 요청](CR-AMR_09-08_11-48_명령_상태_보고_종단_계약.md)
- 결정 일자·근거: 미정. 아래는 2026-09-08 코드 대조 결과와 제안이다.
- 코드 변경 승인: 현재 작업은 patrol_amr_safety 범위. mission 담당 소유 파일 변경·JSON schema 확장은 미승인이며 이번 요청에서 구현하지 않는다.

## 확인된 미연결 입력

`patrol_amr/mission_state.py`의 MissionStateSnapshot과 `mission_status_store.py`의 schema_version=1에는 operational_state·docking_state·scan_state가 없다. safety 쪽 MissionStatusBridge는 mission 상태를 읽지만 나머지 두 enum 축에 값을 전달받지 못한다. 따라서 RobotStatus의 operational_state와 docking_state는 UNKNOWN, scan_state는 빈 문자열로 남는다. 기존 자동시험이 통과했어도 이 세 입력의 실연결 완료를 의미하지 않는다.

## 제안과 요청 작업

1. mission 담당이 운영·도킹 축의 실제 입력 주체와 기존 enum에 대한 전이 조건을 정리한다. 순찰 중이라는 사실만으로 MOVING을, 배터리 충전만으로 DOCKED를 추정하지 않는다.
2. 확정된 축을 mission snapshot에 넣을지 별도 기존 입력을 사용할지 합의한다. snapshot 확장 시 schema 호환성·구버전 처리·재시작·revision·원자 갱신 방법을 같이 정한다.
3. 합의 후 mission 담당이 생산 코드를 반영하고, safety 담당이 MissionStatusBridge와 RobotStatus 변환·Q-02 변경 발행을 연결한다.
4. scan_state 상세 계약은 TBD-IF-003을 따르며 새 문자열을 임의로 추가하지 않는다.

| 대상 | 필요한 작업 | 현재 상태 |
|---|---|---|
| AMR mission 담당 | 입력·전이·저장 경계 제안 및 생산 코드 | 검토 필요·미반영 |
| AMR safety 담당 | 합의된 입력 수신·변환·ROS 시험 | 입력 합의 대기 |
| 관제 | 기존 상태 축의 의미·전이 검토 | 요청 초안, 전달·회신 없음 |
| System monitor | 기존 wire 필드 표시·저장 유지 검토 | 요청 초안, 전달·회신 없음 |
| 비전 | scan 상세 연관은 기존 TBD-IF-003 논의 시 검토 | 이번 입력 연결 구현 변경 없음 |

## 영향·완료 조건

공용 RobotStatus 레이아웃·enum을 유지한다면 관제·System monitor 코드 변경은 불필요할 것으로 판단한다. 다만 실제 코드 대조·상대 확인 전에는 합의 완료로 처리하지 않는다. 기존 UNKNOWN을 측정된 상태로 채우는 것은 운영 판단에 영향을 주므로 전이 조건을 먼저 확정한다. 합의 전에는 현재 UNKNOWN과 빈 문자열을 유지한다.

검증은 각 축 변화가 독립적으로 반영되는지, 잘못된 snapshot이 기존 상태를 손상시키지 않는지, 재시작 후 최신 revision과 ID가 유지되는지, 실제 ROS에서 Q-02 발행 주기를 지키는지 확인한다. 자동시험 계획이며 이번 요청서 작성으로 구현·시험 완료 처리하지 않는다. 실제 mission·로봇 연결 시험은 별도로 남긴다.

## 검토·결정 이력

| 일자 | 단위 | 내용 |
|---|---|---|
| 2026-09-08 | AMR | 현행 snapshot의 누락 축과 담당 범위를 기록. 미정 전이는 구현하지 않음 |

| 2026-09-08 23:22 KST | AMR 사용자 | 사전 합의·참고 자료 없음 확인. 입력·전이 계약 합의 전 연결 구현 보류 |

## 후속 사용자 승인: 임시 규칙으로 코드 연결

2026-09-08 사용자가 미정값을 임의로 정하되 별도로 명시하고 코드를 먼저 완성하도록 승인했다. 생산자 파일은 변경하지 않고 safety 소유 `provisional_status_policy.py`에서 기존 관측·mission snapshot으로 보고 3축을 연결한다. [임시 결정·교체 지점](../development/provisional-amr-status-policy.md)을 따른다. 이는 위 생산자 입력 합의나 공용 TBD 해결을 뜻하지 않는다. 합병 시 명시적 입력으로 교체하는 요청은 유지한다.
